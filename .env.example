# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
# Service Role Key - ONLY for admin operations (reset password, etc.)
# ⚠️ Keep this secret! Never commit to git!
# Service Role Key - ONLY for admin operations (reset password, etc.)
# ⚠️ CAUTION: NEVER use this in client-side code (browsers). 
# If you need admin rights, use Supabase Edge Functions or a backend proxy.
# Ensure this variable is NOT prefixed with VITE_ in your actual .env if you want to prevent accidental exposure, 
# although the client code itself determines if it's bundled.
# CHECK: Search your codebase for usages of this key before deploying.

